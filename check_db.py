from app import create_app; from app.models import db; app = create_app(); ctx = app.app_context(); ctx.push(); from sqlalchemy import inspect; i = inspect(db.engine); t = i.get_table_names(); print('EXISTS:', 'login_attempt_logs' in t); [db.create_all() if 'login_attempt_logs' not in t else None]
