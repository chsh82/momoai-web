# MOMOAI v4.1.0 - 프로덕션 환경변수 템플릿
# 이 파일을 복사하여 .env.production 파일을 만들고 실제 값으로 교체하세요
# 주의: .env.production 파일은 절대 Git에 커밋하지 마세요!

# Flask 설정
FLASK_ENV=production
SECRET_KEY=your-super-secret-key-change-this-in-production-minimum-32-characters

# 데이터베이스 (프로덕션은 PostgreSQL 권장)
# SQLite (테스트용)
# DATABASE_URL=sqlite:///momoai.db

# PostgreSQL (권장)
DATABASE_URL=postgresql://momoai_user:your_password@localhost:5432/momoai_db

# MySQL (대안)
# DATABASE_URL=mysql+pymysql://momoai_user:your_password@localhost:3306/momoai_db

# AI API 키
ANTHROPIC_API_KEY=your-anthropic-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# SMS/알림 API
SMS_API_KEY=your-sms-api-key
SMS_USER_ID=your-sms-user-id
SMS_SENDER=your-phone-number

# 카카오톡 API
KAKAO_API_KEY=your-kakao-api-key
KAKAO_USER_ID=your-kakao-user-id
KAKAO_SENDER_KEY=your-kakao-sender-key

# PWA 푸시 알림 (선택사항)
# VAPID 키 생성: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print(f'Public: {v.public_key.decode()}'); print(f'Private: {v.private_key.decode()}')"
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_CLAIMS_SUB=mailto:your-email@example.com

# 도메인 설정
DOMAIN=momoai.kr
ALLOWED_HOSTS=momoai.kr,www.momoai.kr

# HTTPS/SSL (Let's Encrypt)
SSL_CERTIFICATE=/etc/letsencrypt/live/momoai.kr/fullchain.pem
SSL_CERTIFICATE_KEY=/etc/letsencrypt/live/momoai.kr/privkey.pem
