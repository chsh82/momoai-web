# MOMOAI v4.1.0 - 프로덕션 전용 패키지
# 사용법: pip install -r requirements.txt -r requirements-prod.txt

# WSGI 서버
gunicorn==23.0.0

# 데이터베이스 드라이버
psycopg2-binary==2.9.10  # PostgreSQL (권장)
pymysql==1.1.1           # MySQL (대안)

# 프로세스 모니터링
supervisor==4.2.5

# 로깅
python-json-logger==3.3.1

# 보안
cryptography>=46.0.4

# 성능 모니터링 (선택사항)
# newrelic==10.4.0
# sentry-sdk[flask]==2.24.0
